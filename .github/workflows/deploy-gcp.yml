# ---------------------------------------------------------------------------
# DEPLOY WORKFLOW — DISABLED
# This is a public resource repo. Deployment is intentionally disabled.
# Uncomment the `on` block below when you are ready to deploy to your own
# GCP project.
# ---------------------------------------------------------------------------
name: Deploy to GCP

# on:
#   push:
#     branches: [main]
#     paths:
#       - 'packages/**'
#       - 'Dockerfile'
#       - '.github/workflows/deploy-gcp.yml'
#       - 'infra/gcp/**'
on:
  workflow_dispatch:

env:
  REGION: us-central1
  PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  SERVICE_NAME: media-pipeline-mcp
  ARTIFACT_REGISTRY: media-pipeline-mcp

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run linter
        run: pnpm lint

      - name: Run build
        run: pnpm build

      - name: Run type check
        run: pnpm typecheck

      - name: Run tests
        run: pnpm test

      - name: Run npm audit
        run: pnpm audit --audit-level=high

      - name: Setup Terraform
        if: github.event_name == 'push'
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.7.0

      - name: Terraform Validate GCP
        if: github.event_name == 'push'
        working-directory: infra/gcp
        run: |
          terraform init -backend=false
          terraform validate
          terraform plan -out=tfplan

      - name: Upload Terraform plan
        if: github.event_name == 'push'
        uses: actions/upload-artifact@v4
        with:
          name: tf-plan-gcp
          path: infra/gcp/tfplan

  security-scan:
    runs-on: ubuntu-latest
    needs: [ci]
    steps:
      - uses: actions/checkout@v4

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          scan-ref: '.'
          format: 'sarif'
          output: 'trivy-results.sarif'
          severity: 'CRITICAL,HIGH'

      - name: Upload Trivy results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: 'trivy-results.sarif'

  # -------------------------------------------------------------------------
  # DEPLOYMENT JOB — DISABLED BY DEFAULT
  # This is a public resource repo. Uncomment the block below when you are
  # ready to deploy to your own GCP project.
  # -------------------------------------------------------------------------
  # build-and-deploy:
  #   needs: [ci, security-scan]
  #   runs-on: ubuntu-latest
  #   permissions:
  #     contents: read
  #     id-token: write
  #
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - name: Authenticate to GCP
  #       uses: google-github-actions/auth@v2
  #       with:
  #         credentials_json: ${{ secrets.GCP_SA_KEY }}
  #
  #     - name: Setup Google Cloud SDK
  #       uses: google-github-actions/setup-gcloud@v2
  #
  #     - name: Configure Docker for Artifact Registry
  #       run: gcloud auth configure-docker ${{ env.REGION }}-docker.pkg.dev
  #
  #     - name: Build and push Docker image
  #       env:
  #         IMAGE_TAG: ${{ github.sha }}
  #       run: |
  #         docker build -t ${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.ARTIFACT_REGISTRY }}/${{ env.SERVICE_NAME }}:$IMAGE_TAG .
  #         docker push ${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.ARTIFACT_REGISTRY }}/${{ env.SERVICE_NAME }}:$IMAGE_TAG
  #
  #     - name: Deploy to Cloud Run
  #       run: |
  #         gcloud run deploy ${{ env.SERVICE_NAME }} \
  #           --image ${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.ARTIFACT_REGISTRY }}/${{ env.SERVICE_NAME }}:${{ github.sha }} \
  #           --region ${{ env.REGION }} \
  #           --platform managed \
  #           --allow-unauthenticated \
  #           --set-env-vars NODE_ENV=production